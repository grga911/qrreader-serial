# Pinned for Python 3.6 on Ubuntu 16.04 (Xenial).
# PyInstaller 5+ and recent PyAutoGUI require Python 3.8+.
# PyInstaller 4.10 is the last release that supports Python 3.6.
PyAutoGUI==0.9.53
pyperclipfix>=1.3.0
pyserial>=3.4,<3.6
pyinstaller==4.10
